import React from "react";

const style = {
	background: "#800",
	color: "#fff",
	padding: 12,
};

const Button = () => (
	<button type="button" style={style}>
		Host Button
	</button>
);

export default Button;
