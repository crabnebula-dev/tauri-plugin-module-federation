{
	"name": "example",
	"scripts": {
		"dev": "pnpm --filter example-* --parallel dev",
		"build": "pnpm --filter example-* --parallel build",
		"guests": "pnpm --filter example_guest* --parallel",
		"host": "pnpm --filter example-host* --parallel"
	}
}
